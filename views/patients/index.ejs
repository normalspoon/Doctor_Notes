<%- include('../partials/header') %>
<div>All Patients</div>
<a href="/patients/new" <%- title === 'A' ? 'class="active"' : '' %>>Add new Patient</a>
<table id="list">
    <thead>
      <tr>
        <th>First Name</th>
        <th>Surname</th>
        <th>Date of Birth</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% patients.forEach(function(p) { %>
        <tr>
          <td><%= p.firstname %></td>
          <td><%= p.surname %></td>
          <td><%= p.dob.toLocaleDateString('en-AU') %></td>
          <td><a href="/patients/<%= p._id %>">SELECT</a></td>
          <td>
            <form action= "/patients/<%= p._id  %>?_method=DELETE" method="POST">
            <button type="submit">X</button>
            </form>
          </td>
        </tr>
        <% }); %>
      </tbody>
    </table>


<%- include('../partials/footer') %>
